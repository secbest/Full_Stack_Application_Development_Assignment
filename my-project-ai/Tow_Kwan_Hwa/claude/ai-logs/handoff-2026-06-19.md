# Handoff Summary - 2026-06-19

**Session conducted by:** Claude (claude-sonnet-4-6)
**Project:** EFAR Digital Operations-to-Billing Platform
**Repository:** Full_Stack_Application_Development_Assignment

---

## What We Worked On

### 1. Created CLAUDE.md
Initialized a `CLAUDE.md` file in the repo root to give future Claude Code sessions immediate context on the project without re-reading the requirements documents.

### 2. Updated CLAUDE.md with confirmed tech stack
After the initial creation, the file was updated to reflect the confirmed technology choices, key reference files, and a writing style rule.

### 3. Scaffolded the full submission folder structure
Generated every folder and placeholder file required by `submission-guide.md` for all four students: Jasper, Kwan Hua, Zheng Bao, and Liang Yi.

---

## Key Decisions Made

| Decision | Rationale |
|----------|-----------|
| Student folder names use lowercase kebab-case (`kwan-hua`, `zheng-bao`, `liang-yi`) | Consistent with filesystem conventions; avoids spaces in paths |
| `.gitkeep` added to `frontend/src/` and `backend/src/` only | These are the only folders with no specified files; all other folders contain at least one placeholder `.md` file |
| `architecture-diagram.png` and `er-diagram.png` created as empty files | PNG is binary and cannot be meaningfully generated as a placeholder; files exist so Git tracks the path, but must be replaced with real Mermaid exports |
| `backend/.env.example` pre-filled with all expected keys | Derived from the confirmed tech stack (Supabase, Cloudinary, Gemini, Xero, JWT) so students have a complete reference |
| Writing style: hyphen (`-`) over em dash (`-`) | User-specified rule, recorded in CLAUDE.md and applied throughout all generated documents |
| Existing `.gitignore` was appended to, not replaced | File already contained Claude Code ignore rules (`.claude/*`); merging preserved those entries |

---

## Files Created or Modified

### Modified
- `.gitignore` - appended `.env`, `node_modules/`, and build output rules

### Created
```
CLAUDE.md

design/
  problem-statement.md
  architecture.md
  architecture-diagram.md
  architecture-diagram.png       (empty - needs Mermaid export)
  er-diagram.md
  er-diagram.png                 (empty - needs Mermaid export)
  jasper/
    use-cases.md
    api-documentation.md
    database-schema.md
  kwan-hua/
    use-cases.md
    api-documentation.md
    database-schema.md
  zheng-bao/
    use-cases.md
    api-documentation.md
    database-schema.md
  liang-yi/
    use-cases.md
    api-documentation.md
    database-schema.md

frontend/
  src/.gitkeep
  package.json
  .env.example
  tests/jasper/test-cases.md
  tests/kwan-hua/test-cases.md
  tests/zheng-bao/test-cases.md
  tests/liang-yi/test-cases.md

backend/
  src/.gitkeep
  package.json
  .env.example
  tests/jasper/test-cases.md
  tests/kwan-hua/test-cases.md
  tests/zheng-bao/test-cases.md
  tests/liang-yi/test-cases.md

README.md
deployment.md
```

---

## What Still Needs to Be Done

### Design (Group)
- [ ] Fill in `design/problem-statement.md` - problem, target users, success criteria
- [ ] Fill in `design/architecture.md` - `frontend/src` and `backend/src` folder structures, third-party services, GenAI services, cloud deployment
- [ ] Author the Mermaid diagram in `design/architecture-diagram.md` and export it to `design/architecture-diagram.png`
- [ ] Author the Mermaid ER diagram in `design/er-diagram.md` and export it to `design/er-diagram.png`

### Design (Individual - each student)
- [ ] `design/<name>/use-cases.md` - define use cases with actor, trigger, main flow, edge cases
- [ ] `design/<name>/api-documentation.md` - document owned endpoints with method, path, request/response, error codes
- [ ] `design/<name>/database-schema.md` - define owned tables with fields, types, constraints

### Code
- [ ] Set up the React + Vite frontend in `frontend/src/`
- [ ] Set up the Node.js + Express backend in `backend/src/` with `src/index.js` as the entry point
- [ ] Implement auth and protected routes in the backend
- [ ] Connect Sequelize ORM to Supabase PostgreSQL
- [ ] Integrate Cloudinary for image uploads
- [ ] Integrate Gemini API for GenAI features
- [ ] Integrate Xero API for draft invoice syncing
- [ ] Add real values to `frontend/.env` and `backend/.env` (never commit these)

### Testing (Individual - each student)
- [ ] Write frontend unit tests in `frontend/tests/<name>/*.test.js` (Jest)
- [ ] Write backend unit tests in `backend/tests/<name>/*.test.js`
- [ ] Fill in `test-cases.md` for each student in both frontend and backend
- [ ] Ensure `npm run build` passes for the frontend
- [ ] Ensure `node src/index.js` starts without errors for the backend

### Group Documents
- [ ] Fill in `README.md` - solution description, task allocation per student, local setup steps
- [ ] Fill in `deployment.md` - cloud provider names and public URLs once deployed

### Deployment
- [ ] Deploy frontend (e.g. Vercel)
- [ ] Deploy backend (e.g. Render)
- [ ] Provision Supabase database and run migrations
- [ ] Configure environment variables on all cloud platforms

### AI Logs (Individual - submitted separately)
- [ ] Each student maintains `your-project-ai/<name>/ai-logs/*.jsonl` throughout all phases
- [ ] Each student writes `your-project-ai/<name>/ai-reflection.md` in their own words (no AI drafting)

## Progress Log

### 2026-06-29 - Wave 0: Auth Foundation & Login UI (Kwan Hua)

**Shared infrastructure (group scope)**

- **Login page** (`frontend/src/pages/auth/LoginPage.jsx`) - Full two-panel login screen matching the Figma design: branding panel left, form right. Includes show/hide password toggle, role-based redirect after login (email keyword maps to role home screen), loading spinner, and inline error display.
- **Client-side form validation** - Email format check and minimum password length (6 characters) validated on blur and on submit. Field-level error messages appear below each input with a red border; errors clear as the user corrects their input.
- **Backend DB connection fix** (`backend/src/config/index.js`) - Sequelize was silently ignoring SSL options when given a raw connection URI, causing `ECONNRESET` errors against Supabase. Refactored to parse the URL manually and pass host, port, username, password, and database as discrete options so SSL is always applied. Added connection pooling.
- **Non-fatal DB startup** (`backend/src/index.js`) - The server no longer crashes if the database is unreachable at startup. The connection check runs in the background and logs a warning instead of throwing an unhandled rejection.
- **Auth error masking** (`backend/src/controllers/authController.js`) - The login endpoint now returns "Invalid email or password." for all failures, including internal DB errors, instead of leaking the raw exception message (e.g. `ENOTFOUND`).
- **DB scripts** (`backend/package.json`) - Added `npm run db:sync` (creates tables via Sequelize sync) and `npm run db:seed` (inserts the five demo user accounts).
- **Deployment docs** (`deployment.md`) - Documented the full local dev setup: prerequisites, install steps, env file configuration, DB sync/seed instructions, and demo account credentials.

### 2026-07-01 - Demo Data Seeding (Kwan Hua)

- **Client seed script** (`backend/src/scripts/seed-clients.js`) - Inserts five demo client records (Raffles Medical Group, Marina Bay Sands Expo, ST Engineering, Jurong Island Industrial Corp, Singapore Sports Hub) used as pricing-contract counterparties. Uses `findOrCreate` on `contact_email` so it is safe to re-run without creating duplicates.
- **Intake submission seed script** (`backend/src/scripts/seed-intakes.js`) - Populates the Quotations Specialist's intake queue with sample `intake_submissions` so the booking workflow has data to test against: 2 pending, 4 confirmed, and 2 rejected records with realistic Singapore hospital/venue details. Looks up Camilla's user ID at runtime (`reviewed_by` FK) instead of hardcoding it, and requires `seed-users.js` to have run first. Uses `findOrCreate` on `reference_number` so it is safe to re-run.

### 2026-07-15 - Sidebar UX & Global Typography (Kwan Hua)

Delivered on branch `feature/sidebar-two-line-nav-and-font-sizing`, merged into `main` with `--no-ff` (merge commit `7441677`).

- **Collapsible sidebar** (`frontend/src/layouts/AppLayout.jsx`) - The sidebar now collapses to a 68px icon-only rail and expands back to the full 240px panel via a toggle button (`PanelLeftClose` / `PanelLeftOpen`). Uses a `transition-[width] duration-300 ease-in-out` with `overflow-hidden` so labels clip smoothly instead of popping. Collapsed state is persisted to `localStorage` (`efar.sidebarCollapsed`) and restored on load. In the collapsed state, nav links center their icons and expose the label via a native hover `title` tooltip; the user footer condenses to the user's initial (still linking to `/settings`) plus an icon-only Sign-out button.
- **Two-line nav list items** (`frontend/src/router/routes.js`, `AppLayout.jsx`) - Added a `sub` field to every `NAV_ROUTES` entry (e.g. Invoices -> "Match & sync to Xero", Vendor Invoices -> "OCR & rebates"). The sidebar renders each link as a Material-style two-line item: primary label on top, muted secondary supporting line below. The secondary line is hidden when the sidebar is collapsed.
- **Fluid global font sizing** (`frontend/src/index.css`) - The root `html` font-size now scales with `clamp(14px, 0.223vw + 11.72px, 16px)`, so body text keys off ~14px on smaller laptop screens and grows to 16px on large desktops. Because the Tailwind type scale is rem-based, every text utility scales proportionally without touching individual classes.
- **Verification** - `npm run build` passes on the merged `main`.

**Tooling note (not project code):** An accidental clone of the `anthropics/skills` marketplace repo had been dropped into `backend/skills/`. It was moved out to the personal skills directory (`~/.claude/skills/`, 15 skills after pruning built-in duplicates) and removed from the repo, so nothing skill-related was committed to the assignment.
