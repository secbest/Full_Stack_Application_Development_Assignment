# Session Handoff - 2026-07-02

---

## Session 1 - Planning and Domain Clarification

### What We Worked On

This was a planning and domain clarification session - no code was written. The focus was on understanding the EFAR workflow and aligning on implementation approach before coding begins.

### Key Questions Answered

#### 1. Post-Intake Workflow (AR Flow)
Clarified the full end-to-end flow after a customer submits the public intake form:

1. Customer submits intake form (public route, no auth) - creates `intake_submissions` record
2. Camilla (Quotations Specialist) reviews in Intake Queue - confirms or rejects
3. On confirm - booking created, Ravi (Field Crew) sees job in My Jobs
4. Ravi completes job and submits 4-step Memo Wizard - creates `service_memo`
5. Sarah (AR Specialist) reviews memo in Memo Review Queue - approves or returns
6. On approve - pricing engine auto-matches against `pricing_contracts` table, draft invoice generated
7. Sarah approves invoice and syncs to Xero as a draft

#### 2. Booking Status Meanings
The statuses "Confirmed / In Progress / Completed (Memo Pending) / Invoiced" are **booking lifecycle statuses** (visible in Booking List and Executive Dashboard Fleet tab), NOT AP-specific statuses:

| Status | Meaning |
|---|---|
| Confirmed | Intake approved by Camilla, booking record created |
| In Progress | Ravi is currently on the job |
| Completed (Memo Pending) | Job done but Service Memo not yet submitted - revenue leakage risk, shown as red rows |
| Invoiced | Full AR flow complete - memo approved, priced, invoice synced to Xero |

The AP Dashboard's own stat cards are separate: **Pending / Low Confidence / Synced / Failed** (vendor invoice OCR states).

#### 3. Notifications in AP Account
The bell icon / notifications panel in the AP account is a **placeholder UI element** with no backend function. Not scoped in the project spec. The app handles alerts via:
- Toast notifications (bottom-right, 3s auto-dismiss, green/red)
- Dashboard stat cards as the "needs attention" summary

No notifications table exists in the schema. The icon can be removed or left as a known placeholder.

#### 4. Table Implementation Decision
**Decision: shadcn/ui DataTable (built on TanStack Table v8)**

Rationale:
- Already in the tech stack (shadcn/ui specified in CLAUDE.md)
- Handles sorting, filtering, pagination, row selection out of the box
- Fully composable - custom cells, status badges, row coloring are straightforward
- Consistent with existing design tokens

Screens that need tables and their specific requirements:

| Screen | Key requirements |
|---|---|
| Intake Queue | Time-in-queue coloring (amber >2h, red >4h), [Review] action column |
| Booking List | Risk rows (#FEF2F2) for missing memos, memo status column |
| Memo Review Queue | Overdue rows in red |
| Invoice List | 6 status types, batch approve button |
| Vendor Invoice List | OCR confidence % with color coding |
| Pricing Contracts | Expired rows at 50% opacity |

**Rejected alternatives:** AG Grid / MUI DataGrid (overkill, breaks design token consistency), plain `<table>` (no sorting/pagination).

### Files Changed

None - planning session only.

---

## Session 2 - Figma Audit, Skill Installation, and Prompt Drafting

### What We Worked On

1. Analyzed the shared Figma Design prototype file to extract all screens, node IDs, and student attribution
2. Installed 4 Claude Code skills from GitHub for Figma-related workflow
3. Discussed Figma Make credit strategy and individual vs group prototype workflow
4. Audited what screens Jasper already has vs what is missing from `use-cases.md`
5. Drafted Figma Make prompts for all 4 students covering 16 missing screens total

### Key Decisions Made

#### Figma Make: Continue existing session, never start a new one
**Decision:** Each student must continue their own existing Figma Make conversation to add missing screens. Starting a new chat loses the established design language (color tokens, card styles, sidebar layout, typography) and wastes Figma AI credits on re-establishing context.

**Anchor phrase:** Every prompt must begin with - "Using the same design system, color palette, sidebar layout, and component styles as the screens we built earlier in this conversation," - this locks Figma Make to the student's existing prototype style.

#### Credit preservation strategy
- Generate one screen per prompt, never batch
- Order prompts from simpler to more complex within each student's set
- Use Figma MCP + Claude Code (free) for tweaks and enhancements; reserve Figma Make credits for net-new screen generation only
- The installed skills (`/apply-design-system`, `/audit-design-system`) can improve existing screens without spending Figma credits

#### Prototype workflow is individual, not group
Each student uses their own Figma Make session to generate their individual screens, then pastes the output into the shared Figma Design file. No student should generate screens for another student's feature area.

### Figma File Audit Results

**File:** DUED - FSAD Prototype (`V7CTP56VAgwbEtzSOJt7CI`) - Page: "DUED Individual - Jasper"

**Jasper's existing screens (7 built):**

| Node ID | Screen |
|---------|--------|
| `322:3` | Revenue Exceptions |
| `322:217` | Fleet Tracker |
| `322:372` | Executive Dashboard |
| `322:565` | Invoicing Sync |
| `322:828` | Month-End Cost / Fleet Analytics |
| `322:1128` | System Security |
| `341:302` | Professional Medical Login UI |

**Jasper's missing screens (4 gaps from `use-cases.md`):**

| Screen | Use Cases | Priority |
|--------|-----------|----------|
| Pricing Contracts (list + detail + rules editor) | UC-01, UC-02 | Critical - blocks pricing engine |
| Memo Review Queue | UC-03 | High - Sarah's daily driver |
| Invoice Review & Adjustment | UC-05 | High |
| Bank Feed View | UC-08 | Medium |

**Other students' coverage noted:**
- Kwan Hua: 5x Enterprise Dashboard component frames (library) - needs Xero Settings, Vendor Upload, AP Review, Sync Status
- Zheng Bao: Ingestion Hub, Rebate Review, AP Invoicing, Smart Reconciliation (dark + light variants) - needs Intake Queue, Booking List, public form
- Liang Yi: Quotation/review screens (mismatch with use cases) - needs field crew mobile screens and Executive Dashboard

### Skills Installed

All installed globally to `~/.claude/skills/` - available across all projects after `/reload-plugins`:

| Skill | Source | Command |
|-------|--------|---------|
| apply-design-system | github.com/edenspiekermann/Skills | `/apply-design-system` |
| audit-design-system | github.com/edenspiekermann/Skills | `/audit-design-system` |
| fix-design-system-finding | github.com/edenspiekermann/Skills | `/fix-design-system-finding` |
| sync-figma-token | github.com/firebenders/sync-figma-token-skill | `/sync-figma-token` |

All four require Figma MCP to be connected (authenticated via `/plugin`).

### Figma Make Prompts Artifact

Published a reference document with all 16 prompts:
**URL:** https://claude.ai/code/artifact/0ccce2c3-12c0-451f-acd1-19ec03c4ca2c

Each prompt includes: real client names, job IDs, dollar amounts, validation states, and edge case examples from the use-cases.md files. Students can copy directly from the artifact into their Figma Make sessions.

### Files Changed

| File | Action | Notes |
|------|--------|-------|
| `~/.claude/skills/apply-design-system/` | Created (installed) | Figma design system skill |
| `~/.claude/skills/audit-design-system/` | Created (installed) | Figma audit skill |
| `~/.claude/skills/fix-design-system-finding/` | Created (installed) | Figma fix skill |
| `~/.claude/skills/sync-figma-token/` | Created (installed) | Figma token sync skill |
| `memory/reference_figma_design.md` | Created | Full Figma file node ID map - all 40+ screens with student attribution, component names, EFAR role mapping |
| `memory/MEMORY.md` | Created/updated | Memory index pointing to both Figma reference files |
| `my-project-ai/Jasper/handoff-2026-07-02.md` | Updated | This file |

No application source code was written in either session today.

---

## What Still Needs to Be Done

### Immediate - Figma Prototype (before starting code)

- [ ] Generate 4 missing screens in Jasper's Figma Make session using the prompts from the artifact above
- [ ] Share the artifact link with Kwan Hua, Zheng Bao, and Liang Yi so they can generate their missing screens
- [ ] Each student pastes their generated screens into the shared Figma Design file under their own label section

### Application Code - Frontend

- [ ] Scaffold shared `DataTable` base component using shadcn/ui DataTable pattern
- [ ] Pricing Contracts screen - list, detail panel, pricing rules editor (UC-01, UC-02)
- [ ] Memo Review Queue with Approve/Return actions and correction note flow (UC-03)
- [ ] Invoice Review & Adjustment - two-column layout, editable line items, manual surcharge row (UC-05)
- [ ] Bank Feed view - Xero transaction list with match status column (UC-08)
- [ ] Implement public intake form (POST /api/intake, no auth required)
- [ ] Wire the above screens into the AR Specialist (sarah) routing path

### Application Code - Backend (Jasper's scope)

- [ ] `pricing_contracts`, `pricing_rates`, `surcharge_schedules` tables - migrations and seeds
- [ ] `invoices`, `invoice_line_items` tables - migrations and seeds
- [ ] `POST /api/memos/:id/approve` - triggers pricing engine (UC-04 system flow)
- [ ] `PATCH /api/invoices/:id` - line item edit
- [ ] `POST /api/invoices/batch-approve` - batch approval queue
- [ ] `POST /api/invoices/:id/sync-xero` - Xero push, writes to `xero_sync_logs`
- [ ] `GET /api/xero/bank-feed` - bank feed pull from Xero
- [ ] All `/api/contracts/*` endpoints (CRUD for pricing contracts)

### Dependencies on Teammates (blocked until resolved)

- [ ] `PATCH /api/bookings/:id/status` - Zheng Bao must implement this before Jasper can test invoice sync completing the `completed -> invoiced` transition
- [ ] `xero_sync_logs` table migration - Kwan Hua owns this migration; Jasper writes AR rows to it but cannot run migrations until Kwan Hua merges theirs
- [ ] `service_memos` table with all 11 pricing engine fields - Liang Yi owns; Jasper's pricing engine reads from this

### Testing

- [ ] Unit tests under `frontend/tests/Jasper/` - pricing match logic, invoice status transitions, contract overlap validation
- [ ] Unit tests under `backend/tests/Jasper/` - pricing engine calculations, Xero payload construction, batch approval logic

### Lower Priority

- [ ] Revenue Leakage alert dismissal (UC-09) - write `leakage_dismissed_at` and `leakage_dismissed_reason` to bookings table via Zheng Bao's PATCH endpoint
- [ ] Reports screen (4 tab types, CSV/PDF export) - after core AR flow is working
- [ ] Remove or stub out non-functional notifications bell icon in AP account

---

## Context for Next Session

- All Figma node IDs are in `memory/reference_figma_design.md` - use these to pull specific screen designs via Figma MCP
- Pre-signed JWT tokens for all roles are in `design/jasper/api-documentation.md` - use for Postman/curl testing locally (`DEV_JWT_SECRET=dev-secret-efar-2026`)
- Design tokens are fixed - exact hex values in CLAUDE.md must be used, not approximated
- No email confirmations anywhere - all feedback via toast notifications only
- Xero receives draft invoices only - never finalised invoices
- The `Design login screen for EFAR/` folder in repo root is the Figma Make export - match it exactly
- Build order follows Wave 0-4 in `design/feature-dependencies.md` - check cross-team dependencies before starting Wave 3 (invoices + Xero sync)
