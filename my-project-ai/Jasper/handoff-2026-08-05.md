# Session Handoff - 2026-08-05

---

## Session - Client Feedback Items 1, 3, 4 + Fixture Repairs + Live Debugging + UI Polish

### What We Worked On

Delivered EFAR interim-review action items 1, 3, and Jasper's half of 4 (README's
"Client Feedback - Interim Review" table), through the full brainstorm -> design doc
-> implementation plan -> TDD -> live verification -> PR -> merge cycle, plus two
unrelated pre-existing flaky-test repairs and a round of UI polish driven by
screenshots after the feature was live in the browser.

1. **Brainstormed and wrote a design spec**
   (`docs/superpowers/specs/2026-08-04-client-feedback-items-1-3-4-design.md`) and an
   implementation plan (`docs/superpowers/plans/2026-08-04-client-feedback-items-1-3-4.md`)
   before touching code - scoped to items 1 (live job milestones), 3 (current-job
   hero on My Jobs), 4 (manpower-only memos, model+wizard half only), and the
   `ContractDetailPage` fixture repair. Explicitly out of scope: item 2 (office-hours
   derivation, Kwan Hua, blocked on item 1), item 5 (AP ingestion, Kwan Hua), item 4's
   pricing-engine null-tolerance half (Kwan Hua), item 6 (presentation, group).
2. **Item 1 - live job milestones**: new `job_milestones` table (one row per tap,
   unique on `booking_id`+`milestone_type`, `recorded_at` always server-set) behind
   `POST /api/bookings/:id/milestone`. Milestones are enforced strictly in sequence
   (`activated` -> `arrived_at_location` -> `en_route` -> `arrived_at_destination` ->
   `job_completed`); recording `activated` is now the real "start job" trigger that
   flips a `confirmed` booking to `in_progress` (previously this only happened as a
   side effect of crew assignment). Both `GET /api/bookings/my-jobs` and
   `GET /api/bookings/:id` now return a `milestones` array. Memo wizard Step 1
   pre-fills `job_start_time`/`job_end_time` from the `activated`/`job_completed`
   timestamps (still editable).
3. **Item 3 - one job, not a queue**: `MyJobsPage.jsx` leads with a single "Current
   Job" hero card (earliest `in_progress`, or a `confirmed` job starting within the
   next hour) carrying the milestone stepper. Went through two redesigns after live
   feedback - see Key Decisions.
4. **Item 4 (model + wizard half) - manpower-only standby jobs**: `ServiceMemo.patient_name`/
   `hospital_destination` are now nullable and conditionally required - mandatory
   for `eas`/`mts`, optional (coerced to `null`) for `event_standby`/`workplace_standby`,
   both in the backend Yup validator and the wizard's `buildStep1Schema(serviceType)`.
   A standby job that *does* have a casualty keeps the values. Engine null-tolerance
   in `pricingService.js` remains open for Kwan Hua.
5. **Repaired two pre-existing broken/flaky test fixtures** (part of the user's ask
   to "get fixtures fixed and working"):
   - `ContractDetailPage.test.jsx` queried `getAllByRole('spinbutton')`, but the
     surcharge editor renders Kwan Hua's `NumberStepper` (a `type="text"` input with
     an `aria-label`, no spinbutton role) - fixed by querying the accessible name.
   - `ContractFormPage.test.jsx` flaked ~1 run in 3 under full-suite parallel load.
     Root-caused (via `systematic-debugging`, not guessed): `user.type()` on a
     34-character contract-name field re-rendered the Formik form once per
     keystroke, costing ~3-4s per test against Jest's 5s default timeout. Swapped to
     `fireEvent.change` (already the pattern used for the date inputs in the same
     helper) - dropped those tests to 371-761ms and the file from ~37s to ~8s.
     Confirmed with 6 consecutive full-suite runs, all green.
6. **Live-debugged a real production-adjacent incident mid-session.** After this
   session's backend changes, the user's actual dev backend crashed on the very
   first real request. Traced it in two layers:
   - **Layer 1**: `relation "job_milestones" does not exist` - the real Supabase dev
     database hadn't run `db:sync` since the new table was added. Fixed by running
     `npm run db:sync` + `npm run db:seed:bookings` against the real database (user
     confirmed first, since it's a real-data action).
   - **Layer 2 (the actual bug)**: `listMyJobs` and `getBookingById` had **no
     try/catch at all**, so that one thrown DB error became an unhandled promise
     rejection that crashed the *entire Node process* for every user, not just a
     500 for one request. Wrapped both in try/catch using the existing
     `internalError` helper, added 2 regression tests, and verified live: a
     malformed booking id now returns a clean 500 while `/health` still responds
     immediately after (proving the process survived).
7. **Iterative UI polish from live screenshots** (all under an explicit
   "don't commit until I say so" instruction, then committed once lifted):
   - Milestone stepper redesigned from a vertical 5-row list (too tall) to a
     horizontal breadcrumb, reusing the exact circle+connector pattern already
     established by `memo-wizard/WizardProgressBar.jsx`. A single caption line
     carries the timestamp (defaults to the most recent stage; tap any earlier
     green circle to see its own time) instead of one line per stage.
   - Found and fixed a real bug while investigating an "Upcoming jobs feels
     redundant" complaint: the list had **no status filter at all**, so it showed
     every booking a crew member had ever been assigned - in the user's real
     account, 11 of 13 rows were already `completed`/`invoiced` with nothing left
     to do. Restricted to `confirmed`/`in_progress` only; removed the now-dead
     `completed`/`invoiced` branch in `JobCard`.
   - Answered a design question **without changing code**: recommended against
     auto-filling `job_start_time` from the booking's scheduled time, since that
     would silently reintroduce the exact inaccurate-billing problem item 1 was
     built to close. Offered a placeholder+nudge alternative if the user wants
     less typing friction later.
   - "En Route" renamed to "On the way" (display label only - the `en_route` enum
     value is unchanged in both frontend and backend).
   - Date-filter tabs reordered twice per clarification - ended at
     `All Upcoming, Today, Tomorrow, This Week`.
   - Removed the collapsible "Upcoming jobs" toggle entirely - it was hiding the
     date tabs and job list behind an extra tap, which the user flagged directly.
   - Milestone stepper spacing increased (`space-y-2` -> `space-y-4`, connector
     margins widened) and stage labels made always-visible on mobile (previously
     `hidden md:block`, now shown at a smaller size instead of disappearing).
8. **Full verification after every change**: backend Jest suite (`212` passing by
   end of session), frontend Jest suite (`136` passing), `npm run build` clean.
9. **Opened and the user merged the PR.** `gh` CLI is not installed on this
   machine, so I could not run `gh pr create` directly - built a pre-filled GitHub
   compare URL (`.../compare/main...feature/client-feedback-and-features?expand=1`)
   and gave the user a ready-to-paste title/description (also saved to the session
   scratchpad as `pr-description.md`). The user opened it, filled it in, and merged
   **PR #3** into `main` via GitHub's web UI. Confirmed on `origin/main` at session
   end: `7ae60d5 Merge pull request #3 from secbest/feature/client-feedback-and-features`.

### Key Decisions Made

- **`job_milestones` as a separate child table, not columns on `bookings`.**
  Reasons: `bookings` is Zheng Bao's model and already carries a cross-team note
  about temporary field-ops routes; a child table records *who* tapped and *when*,
  is naturally idempotent (unique constraint per booking+type), and needs no
  `bookings` migration. `recorded_at` is always server-set - the tap itself is the
  event, so backdating isn't possible even by an honest mistake.
- **Recording `activated` is the new "start job" trigger.** Previously a `confirmed`
  booking only became `in_progress` as a side effect of crew assignment (see
  `Booking.js`'s status comment) - there was no real "Start Job" action. Now
  recording the first milestone does this, finally giving the My Jobs "Start Job"
  button (previously permanently disabled) a working action.
- **Current-job hero selection**: earliest `in_progress` job wins; failing that, the
  earliest `confirmed` job scheduled today whose start time is within the next hour
  (matching EFAR's stated ~1-hour call-centre lead time). Completed/invoiced jobs are
  never eligible.
- **Milestone stepper is a breadcrumb, not a list** - deliberately reusing an
  *existing* pattern (`WizardProgressBar`) rather than inventing a second stepper
  style, after the original vertical-list version was flagged as too tall. Tradeoff
  named to the user: only one timestamp is visible at a time (the caption line);
  earlier ones are one tap away on their own circle, not lost.
- **"Upcoming jobs" = has something left to do, not "every booking ever."** A
  completed/invoiced booking's memo is already submitted - that belongs in Memo
  History, not a second, misleadingly-labeled list on My Jobs. This was a genuine
  bug (no status filter existed), not just a UX preference.
- **Declined to auto-fill `job_start_time` from the booking's scheduled time**, even
  though it would reduce typing when no milestone was recorded. The scheduled time
  is the *planned* time set before the job happened, not the real activation time -
  falling back to it would let a wrong value look legitimate and silently
  reintroduce the inaccurate-billing problem item 1 exists to close. No code change
  from this - purely a recommendation the user accepted.
- **`try/catch` audit is a systemic gap, not a one-off fix.** `listMyJobs`/
  `getBookingById` were fixed because they were live-fired this session, but
  `listBookings`/`updateBookingCrew` in the same file already had the pattern -
  meaning other controllers may have the same gap. Flagged as worth a project-wide
  check, not assumed fixed everywhere.
- **Root-caused both flaky-test fixes rather than silencing them** - the
  `ContractDetailPage` fix targets the actual accessible-name mismatch;
  the `ContractFormPage` fix targets the actual per-test time cost (measured
  before/after: ~3-4s -> 371-761ms), not just a bumped timeout.
- **Deployment is explicitly deferred until all functions across the team are
  complete** - the user stated this mid-session; noted in memory so it isn't
  re-litigated or assumed otherwise in a future session.
- **PR merged via GitHub's web UI, not `gh` CLI** - `gh` isn't installed on this
  machine. Rather than search for credentials/tokens to script around that, gave
  the user a pre-filled compare URL and description to complete manually. Worth
  installing `gh` (`winget install --id GitHub.cli`) if this comes up again.

### Files Changed

**Backend - new:**
- `backend/src/models/JobMilestone.js` - the milestone model
- `backend/src/controllers/jobMilestoneController.js` - `recordMilestone`,
  `MILESTONE_SEQUENCE`, `serializeMilestones` (exported for reuse in `bookingController`)
- `backend/src/validators/milestoneValidators.js` - `MILESTONE_TYPES`,
  `milestoneBodySchema`, `bookingIdParamSchema`
- `backend/tests/jasper/jobMilestoneController.test.js` (8 tests),
  `bookingMilestonesInclude.test.js` (7 tests, includes 2 crash-prevention
  regression tests), `serviceMemoManpowerOnly.test.js` (5 tests)

**Backend - modified:**
- `backend/src/controllers/bookingController.js` - `JobMilestone` include on
  `listMyJobs`/`getBookingById`, and both wrapped in try/catch (crash fix)
- `backend/src/controllers/serviceMemoController.js` - notification body falls
  back to the booking reference when there's no patient name
- `backend/src/models/ServiceMemo.js` - `patient_name`/`hospital_destination` now
  `allowNull: true`
- `backend/src/models/index.js` - `JobMilestone` registered + associations
- `backend/src/routes/bookingRoutes.js` - `POST /:id/milestone` route
- `backend/src/validators/index.js`, `serviceMemoValidators.js` - milestone
  validator exports; `requiredForAmbulanceOnly` conditional rule
- `backend/src/scripts/seed-bookings.js` - `BKG-TEST-00005` (manpower-only demo
  booking, scheduled at 14:00 to avoid displacing the hero slot), seeded
  milestones on `BKG-TEST-00001`
- `backend/src/scripts/reset-demo-memo-booking.js` - also clears milestone rows now

**Frontend - new:**
- `frontend/src/components/MilestoneStepper.jsx` - the breadcrumb stepper
- `frontend/tests/jasper/MilestoneStepper.test.jsx` (7 tests),
  `MemoWizardStandby.test.jsx` (4 tests)

**Frontend - modified:**
- `frontend/src/pages/jobs/MyJobsPage.jsx` - hero card, milestone recording,
  always-visible Upcoming jobs section (status-filtered), reordered tabs
- `frontend/src/api/fieldOps.js` - `recordMilestone`
- `frontend/src/validation/serviceMemoValidation.js` - `buildStep1Schema`,
  `isManpowerOnlyServiceType`, `AMBULANCE_SERVICE_TYPES`
- `frontend/src/pages/jobs/memo-wizard/Step1JobDetails.jsx` - milestone pre-fill,
  optional patient fields for standby bookings
- `frontend/src/pages/jobs/memo-wizard/Step2ServiceCharges.jsx` - defaults
  `service_type` from the booking
- `frontend/src/pages/jobs/memo-wizard/MemoWizardPage.jsx` - passes `booking` to
  Step 2, final-submit guard, null coercion for blank patient fields
- `frontend/src/pages/jobs/memo-wizard/Step4StampSubmit.jsx` - dash fallback for
  null patient/destination in the summary
- `frontend/tests/jasper/ContractDetailPage.test.jsx`,
  `ContractFormPage.test.jsx` - the two fixture repairs
- `frontend/tests/jasper/MyJobsPage.test.jsx`,
  `serviceMemoValidation.test.js` - extended/rewritten for all of the above

**Docs:**
- `docs/superpowers/specs/2026-08-04-client-feedback-items-1-3-4-design.md`
- `docs/superpowers/plans/2026-08-04-client-feedback-items-1-3-4.md`
- `README.md` - action-items table updated (1/3 delivered, 4 half-delivered, 2 unblocked)
- `backend/tests/jasper/test-cases.md`, `frontend/tests/jasper/test-cases.md` - new rows

**Git:** 17 commits on `feature/client-feedback-and-features`, pushed, opened as
PR #3, merged into `main` by the user via GitHub's web UI (merge commit `7ae60d5`).
The feature branch still exists on `origin` (not deleted post-merge). The current
local checkout has since moved to a new branch,
`feature/live-data-customer-quotations-field-crew` (0 commits ahead of `main` as
of session end - freshly branched, already contains all of this session's merged work).

### What Still Needs to Be Done

- [ ] **Item 2** (derive `is_office_hours` from the real `activated` timestamp) is
  now unblocked for Kwan Hua - the milestone data it needs exists in
  `job_milestones`. Not started; not this session's scope.
- [ ] **Item 4's pricing-engine half** (null-tolerance in `pricingService.js` for a
  memo with no patient, and confirming standby rate rows exist per contract) is
  Kwan Hua's remaining piece.
- [ ] **`npm run db:sync` is required before this branch works against any other
  existing database** (new `job_milestones` table, nullable-column change on
  `ServiceMemo`) - already run against this session's real dev DB, but any other
  environment (a groupmate's machine, a fresh clone) needs it too.
- [ ] The placeholder+nudge alternative for `job_start_time` (show the scheduled
  time greyed out with a hint, instead of leaving the field fully blank) was
  discussed but not built - only build if the user asks after living with the
  current blank-and-required behavior for a while.
- [ ] No manual on-device (real phone) verification was done this session - all UI
  polish was checked in Chrome DevTools' responsive view per the user's screenshots,
  not an actual mobile browser.
- [ ] `gh` CLI is not installed on this machine - worth installing before the next
  PR is needed, so it doesn't have to go through the manual compare-URL workaround
  again.
- [ ] Deployment is explicitly on hold until all functions across the team are
  complete (see Key Decisions) - don't suggest or begin deployment steps until the
  user says otherwise.

### Context for Next Session

- `main` is at `7ae60d5` as of session end (PR #3 merged); the working branch is
  now `feature/live-data-customer-quotations-field-crew`, which already contains
  everything from this session.
- The design spec (`docs/superpowers/specs/2026-08-04-client-feedback-items-1-3-4-design.md`)
  is the source of truth for *why* `job_milestones` is a separate table and why the
  milestone sequence is enforced strictly - read it before touching anything in
  `jobMilestoneController.js` or the hero-card selection logic.
- Partway through this session, several files' contents briefly appeared reverted
  to their pre-session state in the tool output (likely a transient artifact of
  comparing branches during the GitHub merge process, not real data loss) - this
  was checked against the actual git state at session end and confirmed to be a
  non-issue: `main`/`origin/main` both have the full merge, and the new working
  branch's files were verified (via `grep`) to contain the finished code
  (`CurrentJobHero`, `UPCOMING_STATUSES`, etc.). No action needed, just noting it
  in case anyone reviewing the raw transcript wonders about it.
- The user gave direct, specific UI feedback via screenshots multiple times this
  session (stepper too tall, spacing too tight, labels missing on mobile, Upcoming
  jobs redundant) - expect this pattern to continue as the feature gets used more;
  each one so far has pointed at a real, fixable issue rather than a preference call.
- The user explicitly asked to hold all commits mid-session ("don't commit until I
  say so") and later lifted it - worth remembering that this user sometimes wants
  to batch-review several changes in the browser before they land in git history,
  not commit-per-fix by default.
