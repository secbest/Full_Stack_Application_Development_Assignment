# Session Handoff - 2026-08-06

---

## Session 1 - My Jobs Live Activation Fixes, Fixture Data Cleanup, Booking Status/Delete Review

### What We Worked On

Started from a live bug report on the "My Jobs" screen and worked through three rounds of root-cause-first fixes to the same file, then a data-cleanup pass on the shared dev database, then a review (no code changes) of the booking status lifecycle and the existing delete-for-invoiced feature.

1. **Bug: Ravi couldn't start a newly assigned job ("Starts at 01:58" with no action).** Root cause: `selectCurrentJob()` in `MyJobsPage.jsx` always promoted the crew's `in_progress` job into the single "Current Job" hero slot, and the "Start Job" action only ever lived inside that hero's milestone stepper. A seeded demo booking (`BKG-TEST-00001`) is deliberately kept `in_progress` forever (for memo-wizard rehearsal via `reset-demo-memo-booking.js`), so it permanently hogged the hero slot and any newly confirmed job had no path to activation. Fix: added a "Start Job" button directly to non-hero confirmed jobs in the Upcoming list (`JobCard`), calling the same `recordMilestone(id, 'activated')` the hero stepper uses. TDD'd with a new regression test.
2. **Follow-up: "Current Job" needs to reflect whichever job the crew just started, not the same stale one.** Changed hero selection from "first `in_progress` job found" to "the `in_progress` job with the most recently recorded `activated` milestone timestamp" (new `activatedAt()` helper). Now tapping "Start Job" on a fresh booking makes it the live hero, superseding an older unfinished one. Added a regression test with two simultaneous `in_progress` jobs and updated an earlier test's expectations to match the new (correct) behavior.
3. **Explained the booking status lifecycle** (`confirmed -> in_progress -> completed -> invoiced`) to the user, verified against the actual code rather than the design docs: `jobMilestoneController.js` (`activated` milestone flips confirmed->in_progress), `serviceMemoController.js:137` (memo submission flips ->completed), `invoiceController.js:373` (successful Xero sync flips completed->invoiced, gated on booking already being `completed`). No code changed, but this grounded the following cleanup work.
4. **Cleaned up cluttered sample/fixture data on the shared Supabase dev DB.** Investigated every seed script (`seed-bookings.js`, `seed-more-revenue-invoices.js`, `seed-leakage-demo.js`, `reset-demo-memo-booking.js`) to distinguish Ravi's pure test fixtures (`BKG-TEST-00001..5`, `BKG-2026-00001..4`) from cross-role fixtures that power the MD's Revenue/Leakage reports and the AR Invoice List (`BKG-REV-0001..5`, `BK-LEAK-0001..3`). Confirmed the DB is shared with teammates before touching anything. Deleted only the 9 Ravi-only fixture bookings (and their cascaded `JobMilestone`/`ServiceMemo`/`MemoSignature` rows - no invoices existed on any of them) via a one-off ad-hoc Node script run directly against the DB; deliberately left the cross-role fixtures alone. This was a **data-only** change - no repo files were touched, and it's reversible by rerunning `seed-bookings.js` (idempotent `findOrCreate`).
5. **Bug: "Start Job" appeared on jobs scheduled for a future day** (screenshot showed a "Tomorrow"-filtered confirmed job with an enabled Start Job button). Root cause: the button added in item 1 had no time-eligibility gate at all, unlike the hero's own "due" check. Fix: extracted the hero's eligibility rule into `isDueForActivation(job, now)` (today's date, within the 1-hour activation window, or already past start time) and reused it both in `selectCurrentJob` and as a new `startEligible` prop gating the button in `JobCard`. Two new tests: one confirming a due job still gets the button, one confirming a job scheduled for tomorrow does not.
6. **Reviewed whether "delete invoiced records" should be built** - discovered it already exists (`bookingController.deleteBooking`, gated to `quotations_specialist` + `status === 'invoiced'` only, cascades booking/memo/signature/invoice/line-items/milestones in one transaction; UI in `BookingListPage.jsx` only shows the Delete button for invoiced rows). Advised against adding a parallel delete action to the field crew's Memo History screen (accountability risk - crew shouldn't be able to erase their own submitted-work record). Confirmed by tracing the code that deleting an invoiced booking via Quotations already cascades to remove the memo (and its signature, via a DB-level `ON DELETE CASCADE`) from Memo History as a side effect - no separate cleanup needed there.

### Key Decisions Made

- **Action availability should never be tied to hero-slot occupancy.** Any confirmed/in_progress job the crew is allowed to act on gets its own actionable control on its own card (mirroring how `in_progress` jobs already got "Create Memo" regardless of hero status) - the hero card is a *display* choice, not a gate on what's actionable. This principle drove both item 1 and item 5's fixes.
- **Hero selection = most recently activated, not first-found.** A crew member can legitimately have more than one `in_progress` job at once (nothing in the backend prevents it); the live "Current Job" should track whatever they just started, not whichever job happened to be created first or appear first in the API response.
- **The 1-hour activation window is a single shared rule, not hero-specific.** Extracted `isDueForActivation()` so "is this job due yet" is answered identically everywhere it matters (hero promotion and the Upcoming card's Start Job button), rather than re-deriving a similar-but-subtly-different check in a second place.
- **Data cleanup scope was deliberately narrow given the shared DB.** Confirmed with the user that the Supabase dev DB is shared with teammates before deleting anything, and explicitly limited deletion to bookings that only ever belonged to Ravi's own test fixtures - left every fixture that feeds another role's screens (MD reports, AR invoice list) untouched, even though the user's original ask ("too many samples") would have technically covered them too.
- **No delete capability added to Memo History.** The existing delete-for-invoiced feature (Quotations Specialist only) is the correct place for this capability; giving field crew the power to delete their own submitted memos would undermine the accountability/anti-leakage purpose of the whole memo system. Cleanup of an invoiced record is achieved by deleting the *booking* upstream, not by adding a redundant control downstream.

### Files Changed

**Modified:**
- `frontend/src/pages/jobs/MyJobsPage.jsx` - added `isDueForActivation()` (exported), `activatedAt()`, rewrote `selectCurrentJob()` to pick the most-recently-activated `in_progress` job and to reuse the due-window check for the confirmed fallback; added `handleStartJob()`, `startingJobId` state, and a gated "Start Job" button (`startEligible` prop) on `JobCard` for non-hero confirmed jobs.
- `frontend/tests/jasper/MyJobsPage.test.jsx` - added 4 new tests (start-job-from-Upcoming, most-recently-started-hero, future-day-job-has-no-button, plus updating one existing test's fixture dates/expectations to match the new hero behavior). 11 tests total in this file, all passing.

**No file changes (data-only, direct DB action):**
- Deleted bookings `BKG-TEST-00001` through `00005` and `BKG-2026-00001` through `00004` from the shared Supabase dev DB, plus their cascaded `JobMilestone`, `ServiceMemo`, and `MemoSignature` rows. Nothing in git changed - `backend/src/scripts/seed-bookings.js` was left as-is, so re-running it restores these fixtures if ever needed.

**Verification:** `npx jest` (187/187 passing across all 24 suites) and `npm run build` (clean) both run after the final fix in this session.

**Git:** none of this was committed mid-session - see below for what this handoff commit includes.

### What Still Needs to Be Done

- [ ] **No automated/repeatable script exists for the fixture cleanup done in item 4** - it was a one-off inline Node command, not saved anywhere. If this kind of cleanup is likely to be needed again (e.g. after more live testing clutters My Jobs/Memo History again), consider writing a proper `backend/src/scripts/clean-ravi-fixtures.js` following the existing `reset-demo-*` script conventions.
- [ ] **Bulk-delete-invoiced was suggested but not built.** The user was told the existing single-row delete-for-invoiced could be extended to a "delete all invoiced" bulk action on the Quotations Booking List; no decision was made on whether to build it.
- [ ] **Teammates on the shared Supabase DB haven't been notified** that `BKG-TEST-00001..5` and `BKG-2026-00001..4` no longer exist. Low risk (confirmed no automated tests or other seed scripts depend on those specific reference numbers), but if anyone was mid-manual-QA against one of those bookings, their test data is gone (recoverable via `node src/scripts/seed-bookings.js`).
- [ ] Manual browser click-through of the two "Start Job" fixes (items 1, 2, 5) hasn't been done this session - only Jest/jsdom coverage. Worth a quick real-browser pass before the next demo, especially for the hero-supersedes-hero visual transition.

### Context for Next Session

- Current branch: `feature/live-data-customer-quotations-field-crew`, up to date with `origin` as of session start.
- `isDueForActivation()` and `selectCurrentJob()` (both exported from `MyJobsPage.jsx`) are now the single source of truth for "is this confirmed job startable right now" - if a future change touches job activation timing, start there rather than re-deriving the window logic.
- The Supabase dev DB is shared with teammates (confirmed explicitly this session) - treat any future direct-DB cleanup with the same caution: check what other roles' seed data depends on before deleting, and prefer scoping to reference-number prefixes owned by one flow (`BKG-TEST-*`, `BK-LEAK-*`, `BKG-REV-*` are each a different owner's fixture set).
- Booking status lifecycle for reference: `confirmed` (Quotations creates) -> `in_progress` (crew taps Start Job / `activated` milestone) -> `completed` (crew submits memo) -> `invoiced` (AR's Xero sync succeeds). AR review/return/resubmit cycles happen entirely within `completed` - the booking status itself doesn't move during memo review.

---

## Session 2 - Managing Director Dashboard: Real Data Across Accounts Management, Executive Dashboard, and Reports

### What We Worked On

The user's stated problem was that the Managing Director dashboard "looks too simple and not informative enough," plus a strategic question (mirror Xero's own metrics, or show data unique to this platform?) and a hard constraint: no sample/mock data anywhere before the app goes live. Went through the full cycle - brainstorm -> audit -> design spec -> three implementation plans -> `subagent-driven-development` execution of all three, each with its own final whole-branch review and fix pass - across the whole MD area (Accounts Management, Executive Dashboard, Reports).

1. **Audited the actual current state before proposing anything.** Dispatched an explore agent rather than guessing: found the Fleet Overview and Expense Summary dashboard tabs and the Revenue Leakage page were already fully live against real data (contradicting the "it's all samples" framing), while Reports' Billing Cycle/Leakage History/Vendor Expenditure tabs, the Revenue tab's service-type donut, and the entire Accounts Management screen were 100% hardcoded mock arrays with no backend behind Force Logout/Unlock/Edit at all.
2. **Answered the Xero-metrics strategy question directly**: recommended surfacing pipeline/operational metrics Xero can't show (cycle time, sync health, leakage) rather than duplicating Xero's own financial reports, since Xero remains the master ledger per the project's own domain rules. User agreed.
3. **Brainstormed and wrote a design spec** (`docs/superpowers/specs/2026-08-06-md-dashboard-real-data-design.md`) covering session/lockout data model changes, new analytics endpoints, and the Reports-page real-data fixes, after several rounds of `AskUserQuestion` (lockout session-tracking depth, which new KPIs mattered most, how much of the Reports page to fix).
4. **Found and resolved two cross-attribution issues before writing any plan**, both flagged proactively rather than discovered mid-implementation:
   - `frontend/tests/liang-yi/Management.test.jsx` already existed under a different student's folder and asserted the OLD mock behavior (e.g. "Edit makes no backend call yet") that this work would break. Asked the user; they chose to have this session edit it directly.
   - The Revenue tab's service-type breakdown could have been built by extending `GET /api/invoices` (owned by a different teammate, Kwan Hua). Designed a new endpoint inside the already-Jasper-owned `dashboardController.js` instead, specifically to avoid touching her file/tests.
5. **Wrote three implementation plans**, decomposed by the user's own choice to execute as "both, as one combined plan" but still split into three independently-reviewable documents:
   - `docs/superpowers/plans/2026-08-06-md-session-tracking-accounts-management.md` (8 tasks)
   - `docs/superpowers/plans/2026-08-06-md-dashboard-analytics-endpoints.md` (6 tasks)
   - `docs/superpowers/plans/2026-08-06-md-reports-real-data.md` (11 tasks, depends on the second plan's `cycle-time` endpoint)
6. **Executed Plan 1 (session tracking & Accounts Management) via `subagent-driven-development`** - fresh implementer subagent + task-reviewer subagent per task, all 8 tasks approved. The final whole-branch review then found a real Critical bug no single task's review could see: `updateUser` changed a user's role without bumping `token_version`, leaving a demoted Managing Director with full admin privileges on their existing token for up to 8 hours. Fixed, plus three Important findings (a locked-and-still-active account couldn't be Force Logged Out; the public `POST /auth/register` accepted an arbitrary role, undercutting the new role gate; `authenticate()`'s `next()` was inside its own try/catch). A re-review of that fix pass caught one more real regression (password change bumped `token_version` server-side but the frontend never stored the re-signed token, so changing your password would immediately log you out) - fixed and re-verified.
7. **Executed Plan 2 (Executive Dashboard analytics endpoints)** - 6 tasks, all approved. Manual verification against the live Supabase dev database (after a transient `ECONNRESET` cleared up) confirmed all four new endpoints (`cycle-time`, `xero-health`, `revenue-trend`, `top-clients`) return real data. The final review caught a genuine timezone bug (local-time vs UTC mismatch in `revenue-trend`'s weekly bucketing would have split one week's revenue across two chart points) and a `top-clients` endpoint whose booking count was both an unbounded full-table scan and didn't match what its own column header claimed (counted all bookings regardless of status, while revenue only counted synced invoices) - both fixed and re-verified, plus two small polish fixes (a stale comment, a misleading column header).
8. **Executed Plan 3 (Reports page real-data fixes)** - 11 tasks, all approved, including two places (Tasks 8 and 9) where an implementer correctly advanced a shared function signature or render call site ahead of where the plan text assumed a later task would do it, specifically to avoid leaving the file in a broken intermediate state - each verified correct by its own task review. Manual DB verification confirmed the two new endpoints (`revenue-by-service-type`, `leakage-history`); a live Playwright browser check (production build + real backend + real seeded DB, logged in as `doris@efar.com.sg`) confirmed all four Reports tabs render correctly with no console errors. The final whole-branch review then found real cross-task architecture issues: a shared `Promise.all` meant one failing analytics endpoint blanked three healthy tabs (and could make the Revenue tab show a false "no invoices" message or stale data from a previous period); a timezone mismatch meant the Revenue tab and the other three tabs could silently disagree about what "this quarter" covers; and CSV/PDF export could ship a file that contradicted an error already showing on screen. Fixed with `Promise.allSettled` + four independent per-dataset error states, a single shared date-window helper, and an extended export gate - re-reviewed clean, then two more small robustness fixes applied directly (clearing stale errors up front, a trailing `.catch` safety net).
9. **Fixed two stray leftover dev-server processes mid-session** (`node -e require('./src/index.js')` instances left running on port 3000 by earlier automated verification steps, blocking the user from starting their own backend) and diagnosed two separate transient Supabase `ECONNRESET` outages as real infrastructure flakiness, not application bugs, by retrying with backoff and cross-checking against a second endpoint before concluding it had cleared.

### Key Decisions Made

- **Xero-metrics strategy**: surface pipeline/operational metrics (cycle time, sync health, leakage) rather than mirroring Xero's own financial reports - Xero remains the master ledger, and duplicating its reports would be redundant and out of scope for this platform's stated problem (manual transcription -> revenue leakage -> billing bottlenecks).
- **Lightweight session tracking, not a full sessions table.** Five new columns on the existing `User` model (`token_version`, `last_login_at`, `last_active_at`, `failed_login_count`, `is_locked`) rather than a dedicated sessions table with per-device tracking - sufficient for a single-org POC, avoids new schema and join overhead.
- **Fix all four Reports tabs, not just the ones that overlap with new dashboard KPIs** - none of them can carry mock data into a live deployment, per the user's explicit no-samples constraint.
- **Edit Liang Yi's pre-existing test file directly** (user-authorized, confirmed before touching it) since her assertions described the exact mock behavior this work removes; **kept the service-type endpoint inside Jasper-owned files** rather than extending Kwan Hua's `invoiceController.js`, for the same cross-attribution reason in the opposite direction.
- **Lockout auto-expiry/rate-limiting explicitly deferred, kept as originally designed.** The final review flagged that manual-unlock-only lockout has no auto-expiry and lets an outsider enumerate/lock any account with 5 requests - user chose to leave this as designed for now rather than expand scope, since it was already an approved decision in the spec.
- **`POST /auth/register` gated behind `managing_director` auth immediately, in the same branch** (not deferred) - pre-existing code, technically outside this plan's diff, but the user chose to fix it now since this branch is what made the hole load-bearing (the new role-gated `GET /api/users` response is only as strong as the weakest way to obtain the `managing_director` claim, and self-registering with an arbitrary role was that weakness).
- **Both known-Important findings from every final review were fixed before moving on**, never merged past with just a note - each fix pass was itself re-reviewed by a fresh reviewer rather than trusting the fixer's own report.
- **Manual browser/live-DB verification was treated as real evidence to gather, not skip** - when an implementer subagent deferred Task 10's browser click-through to "no test exists yet, static trace is enough," the controller (this session) ran an actual Playwright check against a production build and the real backend afterward rather than accepting the deferral as final.

### Files Changed

**Backend - modified:**
- `backend/src/models/User.js` - `token_version`, `last_login_at`, `last_active_at`, `failed_login_count`, `is_locked` columns
- `backend/src/utils/token.js` - `signToken` embeds `token_version`
- `backend/src/middleware/index.js` - `authenticate` checks `token_version`, stamps `last_active_at` (throttled), `next()` moved outside its own try/catch
- `backend/src/controllers/authController.js` - login lockout after 5 failed attempts, `ACCOUNT_LOCKED` (403)
- `backend/src/controllers/userController.js` - `listUsers` extended (MD-gated), new `updateUser`/`forceLogout`/`unlockUser`, role-change token revocation, self-guard on `forceLogout`, `updatePassword` re-signs and revokes other sessions
- `backend/src/validators/userValidators.js`, `backend/src/routes/userRoutes.js`, `backend/src/routes/authRoutes.js` - new schema/routes, `/auth/register` gated to `managing_director`
- `backend/src/controllers/dashboardController.js` - new `cycleTime`, `xeroHealth`, `revenueTrend`, `topClients`, `revenueByServiceType`, `leakageHistory`; `vendorExpenses` (pre-existing, reused) wrapped in try/catch
- `backend/src/routes/dashboardRoutes.js`, `backend/src/validators/dashboardValidators.js`, `backend/src/validators/index.js` - new routes/schemas for all six new endpoints

**Backend - new tests:** `tokenAndAuthMiddleware.test.js`, `authControllerLockout.test.js`, `authRoutes.test.js`, `dashboardCycleTime.test.js`, `dashboardXeroHealthRevenueTopClients.test.js`, `dashboardRevenueByServiceType.test.js`, `dashboardLeakageHistory.test.js`, `dashboardVendorExpenses.test.js` (plus extensions to `userController.test.js`) - all under `backend/tests/jasper/`.

**Frontend - modified:**
- `frontend/src/pages/dashboard/Management.jsx` - real `GET /api/users` fetch, Edit/Force Logout/Unlock/Remove all wired to real endpoints, `isOnline` tracked independently of the collapsed status label
- `frontend/src/pages/dashboard/FleetOverviewTab.jsx` - 5th KPI card, Xero Sync Health card, revenue trend chart, top-clients table, all-time/trailing-window captions
- `frontend/src/pages/dashboard/ReportPage.jsx` - all four tabs rebuilt on real data (`ReportRevenue`, `ReportBillingCycle`, `ReportLeakage`, new `ReportVendorExpenditure`), shared date-window helper, `Promise.allSettled` with four independent error states, extended export gate
- `frontend/src/pages/settings/SettingsPage.jsx` - stores the re-signed token after a password change
- `frontend/src/api/users.js`, `frontend/src/api/fieldOps.js` - new wrapper functions for all six new/extended endpoints

**Frontend - tests:** `frontend/tests/liang-yi/Management.test.jsx` (updated for real behavior, user-authorized), new `ManagementSessionActions.test.jsx`, `FleetOverviewTab.test.jsx`, `ReportPageRealData.test.jsx`, plus `SettingsPage.test.jsx` extended for the token-storage fix - all new files under `frontend/tests/jasper/`.

**Docs:**
- `docs/superpowers/specs/2026-08-06-md-dashboard-real-data-design.md`
- `docs/superpowers/plans/2026-08-06-md-session-tracking-accounts-management.md`
- `docs/superpowers/plans/2026-08-06-md-dashboard-analytics-endpoints.md`
- `docs/superpowers/plans/2026-08-06-md-reports-real-data.md`

**No schema migrations** - all `User` column additions applied via `npm run db:sync` (`sequelize.sync({ alter: true })`), matching this repo's existing no-migration-per-column convention.

**Verification:** backend `npx jest` 38/38 suites (391 tests) and frontend `npx jest` 27/27 suites (199 tests) both green as of session end; `npm run build` (frontend) clean; live Playwright browser check against a production build + real backend + real seeded DB confirmed all four Reports tabs.

**Git:** 31 commits on `feature/managing-director-updates` (`e4966d7` through `5e1c38c`), each task individually reviewed before the next started, plus a final whole-branch review and fix pass per plan. Not merged to `main` and no PR opened yet - the user was asked to choose (merge locally / PR / keep as-is / discard) but the conversation moved to writing this handoff before a decision was made.

### What Still Needs to Be Done

- [ ] **The branch has not been merged, PR'd, or otherwise finished.** `feature/managing-director-updates` is 31 commits ahead of wherever it branched from `main`, fully green, but still sitting as a local branch. Next session should re-ask which of merge/PR/keep-as-is/discard the user wants.
- [ ] **Lockout has no auto-expiry or rate-limiting, and 403-vs-401 lets an outsider confirm any email exists** - explicitly deferred by the user this session (see Key Decisions), not forgotten. Revisit only if asked.
- [ ] **`backend/tests/jasper/test-cases.md` and `frontend/tests/jasper/test-cases.md`** were not updated with this session's many new test files - a reviewer flagged this as pre-existing drift this session continued rather than caused, but it's worth a catch-up pass before submission if that master list is graded/checked.
- [ ] **Live browser verification only covered "This Quarter" with mostly-zero seed data** (Leakage History and Vendor Expenditure both showed genuinely-correct empty states, cross-checked against the pre-existing Revenue Leakage endpoint, but never a populated row). Before relying on this for a demo, seed one invoice with real `unpriced_surcharges` and one approved vendor invoice inside the current quarter and re-check both tabs plus their CSV export against the real numbers.
- [ ] **Several Minor findings across all three final reviews were explicitly left as follow-ups, not fixed**: inconsistent money-formatting across the Reports screen's cards (four different conventions - no fixed decimals in one place, thousands separators inconsistently applied); the Revenue donut's slice colors are keyed by rank not `service_type`, so they can reshuffle between periods; the Vendor Expenditure tab filters on `invoice_date` while the other three filter on `created_at` (different semantics, undocumented); no test exercises the CSV/PDF export gate itself; the `stage_averages_days` field `cycle-time` computes is currently unused by any consumer.
- [ ] **`fleetOverview`'s own period-range helper (`resolvePeriodRange`/`startOfDay`) still uses local time**, the same class of bug Fix 1 in Plan 3's final review removed from `revenue-trend` - it was explicitly out of scope for that fix (different endpoint, not touched by this session's plans) but is a known remaining inconsistency in the same controller file.
- [ ] **Any other environment running this codebase (a groupmate's machine, a fresh clone, a deploy target) needs `npm run db:sync` run against its database** before this branch's code will work at all - `authenticate()` now selects `token_version`/`last_active_at` on every single authenticated request, so without the sync, every authenticated route in the whole app (not just the MD screens) returns 500.

### Context for Next Session

- **The full task-by-task record of this session's `subagent-driven-development` execution** (which implementer/reviewer ran on which commit, every finding, every fix) is in `.superpowers/sdd/progress.md` (git-ignored scratch) as of session end - useful if anyone wants to trace exactly why a particular line of code looks the way it does, but it won't survive a `git clean -fdx`.
- **This session's MD-dashboard scope (Accounts Management, Executive Dashboard analytics, Reports) was explicitly done under Jasper's name per the user's direct answer this session**, distinct from the earlier Wave 2A handover note (which covered Liang Yi's Field Operations/Executive Dashboard *build*, not this real-data follow-up work). If picking this up cold, don't assume the earlier handover note's attribution applies to this session's specific files.
- **Three design/plan docs are the source of truth for *why*** the session/lockout model, the six new analytics endpoints, and the Reports-tab rewrites are shaped the way they are - read the spec's "Decisions" section first if resuming cold, since it records the Xero-metrics-strategy and session-tracking-depth choices that shaped everything downstream.
- **Two other students' files/scope were deliberately protected this session** - `frontend/tests/liang-yi/Management.test.jsx` was edited (with permission) rather than left alone; Kwan Hua's `backend/src/controllers/invoiceController.js` was deliberately NOT touched (a new endpoint was added elsewhere instead). Keep both in mind before either file gets touched again.
- **Stray dev-server processes on port 3000 were cleaned up twice this session** (leftover `node -e require('./src/index.js')` instances from earlier automated verification steps) - if `npm run dev`/`node src/index.js` in `backend/` ever reports `EADDRINUSE` on port 3000 again, check for an orphaned process before assuming something is wrong with the app itself.
- **Supabase connectivity was transiently flaky twice this session** (`ECONNRESET` on `sequelize.authenticate()`), both times resolving within a couple of retries with backoff - treat a single connection failure as possibly transient infrastructure, not necessarily a code regression, before investigating further.
