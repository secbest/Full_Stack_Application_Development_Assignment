# Session Handoff - 2026-08-08

---

## Session 1 - Fleet Map Legend and Memo Transfer-Type Pre-fill (on `fix/hotfix-bugs-Jasper`)

### What We Worked On

Two small, user-reported bugs, fixed on the existing `fix/hotfix-bugs-Jasper` branch.

1. **Fleet Map legend missing.** The Managing Director's Fleet Tracker map (`FleetMap.jsx`) renders crew as colored pins (green/amber/blue/grey) but had no key explaining what each color meant. Added a `MapLegend` sub-component rendered below the map itself, so it's automatically present both in the small dashboard map and the "expand" modal (both reuse the same `FleetMap` component). Required restructuring `FleetMap`'s outer JSX from a single absolutely-positioned map div to a flex column (map area `flex-1 min-h-0`, legend below it, fixed height) so the legend doesn't get clipped or steal space from the map's `ResizeObserver`-driven sizing.
2. **Field crew memo wizard left "Transfer Type" blank instead of carrying over what Quotations already picked.** Initially misdiagnosed - `Step2ServiceCharges.jsx`'s `transfer_type` field genuinely defaults to `''` unconditionally, which looked correct in isolation (mirrors the "field crew must select it" requirement in `CLAUDE.md`). Asked the user to clarify twice; the real complaint was the opposite: Quotations already selects a `quoted_transfer_type` during Intake Queue review (`IntakeQueuePage.jsx`'s "Transfer Type" field, used for AR price quoting), and the crew's memo should pre-fill from that value (same pattern already used for `service_type`, which defaults from `booking.service_type`) rather than starting blank and making the crew re-pick something Quotations already decided. Fixed by:
   - Adding `quoted_transfer_type` to the `GET /api/bookings/:id` response (`getBookingById` in `bookingController.js`) - it wasn't being returned to the Memo Wizard's booking-detail fetch at all.
   - Defaulting `Step2ServiceCharges.jsx`'s `transfer_type` initial value to `booking?.quoted_transfer_type` when there's no `wizardData` value yet. Still fully editable if the crew needs to change it.

### Key Decisions Made

- **Don't trust the literal wording of a bug report over what the code actually does - but don't guess past a genuine ambiguity either.** The first read of "field crew needs to fill the transfer type (not automatic)" matched the existing code exactly (already blank/manual), which meant either the report was about something else, or a different field/path was involved. Rather than picking a direction and editing based on a guess, asked the user directly (twice, since the first clarifying question's answer - "Something else" - still wasn't specific enough) before touching code that already looked correct on its face.
- **Pre-fill `transfer_type` from `quoted_transfer_type`, mirroring the existing `service_type` pattern**, instead of inventing a new mechanism - `Step2ServiceCharges.jsx` already had a working precedent (`initialValues.service_type || booking?.service_type || ''`) for "pre-fill from what was already decided upstream, but let the crew override it."
- **Legend lives inside `FleetMap.jsx` itself, not in the page that embeds it**, so both call sites (`FleetTrackerPage.jsx`'s minimap and its expand-modal) get it for free with no duplicated markup.

### Files Changed

- `backend/src/controllers/bookingController.js` - `getBookingById` now includes `quoted_transfer_type` in its response.
- `frontend/src/pages/jobs/memo-wizard/Step2ServiceCharges.jsx` - `transfer_type` initial value now defaults to `booking?.quoted_transfer_type` (falls back to `''` if Quotations never set one).
- `frontend/src/components/FleetMap.jsx` - added a `MapLegend` component (color dot + label per crew status) rendered below the map; restructured the component's root layout to a flex column to accommodate it without breaking the map's sizing/`ResizeObserver` behavior.

**Verification:** changes were reviewed by reading the affected files and tracing the data flow (Intake Queue's `quoted_transfer_type` -> `Booking` model -> `getBookingById` -> Memo Wizard's `booking` prop -> `Step2ServiceCharges` initial values) end-to-end. **No test suite run, no manual browser click-through, and no backend restart/live check were done this session** - see below.

### What Still Needs to Be Done

- [ ] **No automated test coverage exists for either change.** `bookingController.js`'s `getBookingById` and `Step2ServiceCharges.jsx` don't currently have tests asserting on `quoted_transfer_type`/`transfer_type` pre-fill behavior - worth adding before this is trusted long-term, especially since this is exactly the kind of "value flows through 4 layers" logic that's easy to silently break later.
- [ ] **Not manually verified in a running app.** The backend change requires a server restart to take effect (this project runs on plain `node`, not `nodemon` - see `CLAUDE.md`'s "Backend Dev Server Management" section) and that restart wasn't done this session. Should click through: Intake Queue review (set a transfer type) -> confirm booking -> assign crew -> My Jobs -> Start Job & Create Memo -> Step 2 should show the transfer type pre-selected.
- [ ] **Map legend not visually verified in a browser** - only reasoned about via the JSX/flex layout change. Worth a quick look at both the small Fleet Tracker map and the expanded modal to confirm the legend doesn't wrap awkwardly or crowd the map at the 480px row height.
- [ ] **This session's commit and merge into `main` was requested by the user but not yet completed as of this handoff being written** - check the git log immediately after this file for whether it landed.

### Context for Next Session

- `Booking.quoted_transfer_type` (and its siblings `quoted_base_amount`, `quoted_time_of_day`) already existed on the model and were already returned by the *list* bookings endpoint (`listBookings`) - they just weren't included in the single-booking `getBookingById` response the Memo Wizard actually calls. Worth checking `getBookingById` against `listBookings`'s field list if another "field crew screen is missing a field Quotations already set" report comes up - they'd drifted apart.
- `FleetMap.jsx`'s legend uses the same `STATUS_COLORS`/`STATUS_LABELS` exports the rest of the Fleet Tracker page (`FleetTrackerPage.jsx`'s stat cards and Crew Roster panel) already use, so there's now a fourth place reading from that single source of truth - if the status vocabulary or colors ever change, all four update from one edit.

---

## Session 2 - Login Page "Forgot Password" Added, Then Removed (on `fix/hotfix-bugs-Jasper`)

### What We Worked On

A short back-and-forth on the login page's "Forgot password?" affordance, ending in its removal.

1. **User reported the login page had no "forgot password" option.** Investigation found this wasn't quite true - `LoginPage.jsx` already had a "Forgot password? Contact your administrator." line, but it was static, non-clickable text with no actual way to act on it.
2. **First fix: made it actionable.** Since the project has no email service (`CLAUDE.md` explicitly rules out email confirmations/flows), a real self-service reset (reset email + token) was out of scope. Instead, turned the existing text into a `mailto:` link pre-addressed to the Managing Director (`doris@efar.com.sg`, the account admin who already has password-reset/unlock actions on the Accounts Management screen), with a pre-filled subject line.
3. **On reflection, the user decided the whole thing wasn't worth keeping** for the final review presentation, given it's tangential to the project's actual problem statement (AR/AP billing automation, not account-recovery UX). Removed the entire "Forgot password?" paragraph from `LoginPage.jsx` rather than leaving it hidden behind a flag or commented out.

### Key Decisions Made

- **A `mailto:` link, not a new backend flow, was the right scope for "add forgot password."** Building real self-service password reset would mean introducing email sending, which directly contradicts this project's documented "no email service" design (`CLAUDE.md`). A `mailto:` link uses the user's own email client and needs zero backend changes, staying consistent with that constraint.
- **When a feature doesn't serve the presentation's core narrative, cut it rather than gold-plate it.** The user's own judgment call - after seeing the fix in place, decided a password-recovery affordance doesn't relate to the assignment's problem statement (billing/invoicing automation) and would be a distraction in a final review, so it was deleted outright instead of kept "just in case."
- **Deleted cleanly, no dead code left behind.** The `<p>` block (line, link, and its styling) was removed in full - no commented-out remnant, no unused import, matching the project's stated preference for deleting confirmed-unused code rather than leaving backwards-compatibility scaffolding.

### Files Changed

- `frontend/src/pages/auth/LoginPage.jsx` - added a `mailto:doris@efar.com.sg` "Forgot password?" link below the sign-in form, then removed that entire block in the same session once the user reconsidered its relevance to the presentation.

**Net effect on this file vs. the start of the session: no change** - the file ends the session identical to how it started (the add-then-remove cancelled out), aside from whatever else already changed it in Session 1 (not touched again here).

**Verification:** none needed beyond re-reading the final JSX - a straightforward deletion of a self-contained `<p>` block with no other references to the removed link/text elsewhere in the codebase.

### What Still Needs to Be Done

- [ ] **No functional account-recovery path exists on the login page as of this session's end** - a locked-out or forgetful user has no on-screen guidance at all now (previously at least static "contact your administrator" text existed). If this becomes a real concern outside the presentation context, revisit either the `mailto:` link approach from earlier in this session or a simpler static text-only note.
- [ ] **Items still open from Session 1** (no test coverage for the transfer-type pre-fill or map legend, no live browser verification of either, no backend restart done to confirm the `quoted_transfer_type` field flows through) remain open - untouched this session.

### Context for Next Session

- If "forgot password" comes up again, the relevant context is already captured above: no email service exists in this stack (`CLAUDE.md`), Accounts Management (Managing Director role) already has unlock/reset-adjacent actions for locked-out users, and a `mailto:` link is the lowest-effort option that respects that constraint - it was tried and explicitly rejected only for being off-topic for the presentation, not for being broken.

---

## Session 4 - Fixed a Singapore-Timezone Bug Making "Today" Silently Drop Early-Morning Records (on `fix/hotfix-bugs-Jasper`)

### What We Worked On

User reported that "Today" wasn't updating on the Dashboard and Reports pages. Traced this to a real, previously-unnoticed timezone bug rather than a UI/refresh problem.

1. **Reproduced live rather than guessing from code alone.** The user's first description ("Numbers don't change when I click Today") matched neither Fleet Overview's Today/This Week/This Month tabs nor Reports' Today/This Month/This Year/Custom period buttons on a code read - both looked like they should refetch correctly on period change. Started both dev servers (both had stopped between messages since the last session) and drove the app with Playwright, logged in as Doris (MD), and confirmed: switching periods on both screens *did* correctly trigger new fetches with different results - so the bug wasn't stale state or a broken click handler.
2. **Found the actual defect by comparing invoice timestamps against the query window.** Fetched the raw invoice list via the API and noticed invoices with `created_at` like `2026-08-07T17:24:39.579Z` - which is 2026-08-08, 01:24am **Singapore time** (UTC+8), i.e. genuinely "today" from the business's perspective, but stored as "Aug 7" in UTC. Compared this against how `ReportPage.jsx` and `dashboardController.js` built their "today" query boundary: both took the LOCAL (Singapore) calendar-date string (e.g. `"2026-08-08"`) and appended a hardcoded `T00:00:00.000Z`/`T23:59:59.999Z` - which reads as **8am Singapore time**, not midnight. Net effect: any record created between local midnight and 8am was silently excluded from "Today," and the window was shifted 8 hours later than the real Singapore day. This is the mechanism behind the report: checking the dashboard in the early morning (or any time, really, for that specific 8-hour band) made recently-created data look missing under "Today."
3. **Fixed it in both places the bug independently existed:**
   - Backend (`dashboardController.js`): added `sgDayStart`/`sgDayEnd` helpers that anchor a bare `YYYY-MM-DD` string to Singapore local time (`+08:00`) instead of UTC, and applied them to the 4 endpoints that compare a date-only boundary against a TIMESTAMP column - `revenueLeakage`, `cycleTime`, `revenueByServiceType`, `leakageHistory`. (`vendorExpenses` and `fleetOverview` were untouched - they filter DATE-only columns, `invoice_date`/`scheduled_date`, which have no time component to shift.)
   - Frontend (`ReportPage.jsx`): the invoice-list fetch had the identical bug independently - it reconstructed a local Y-M-D string and appended a hardcoded `Z`. Replaced with `dateRange.startDate.toISOString()`/`.endDate.toISOString()`, which correctly converts the already-local `Date` objects to their true UTC instant.
4. **Verified the fix live, then with both test suites.** Re-ran the same Playwright reproduction against the restarted backend: Reports' "Today" tab went from `$0.00 / 0 invoices` to `$65.40 / 3 invoices` (the early-morning invoices that were previously excluded), while Fleet Overview's Today/This Week/This Month (unaffected, DATE-only columns) still worked as before. Ran the full backend (43/43 suites, 468 tests) and frontend (32/32 suites, 223 tests) Jest suites - all green, confirming the fix didn't regress anything relying on the old (wrong) behavior.
5. **Started, then cleanly stopped, both dev servers used for verification** - asked the user whether to leave them running for a manual look or stop them; user chose to stop, so both were killed by PID once testing was done, per this project's standing port-cleanup rule.

### Key Decisions Made

- **Reproduce before diagnosing, especially when the code reads as correct.** The user's first-pass description didn't match what the code appeared to do on inspection (both period controls looked like they should refetch fine) - rather than guessing at a second theory from code alone, actually running the app end-to-end (Playwright + real login) surfaced the true mechanism: the numbers *do* update, they're just wrong for "Today" specifically, in a way invisible unless you compare a raw timestamp against the actual query boundary being sent.
- **Fixed at the point where a calendar-date string meets a TIMESTAMP column, not by changing what "today" means everywhere.** `resolvePeriodRange` (used for Fleet Overview's `period=today/this_week/this_month`) already computes correctly, because it builds boundaries from real local `Date` objects on a machine whose OS timezone happens to already be `Asia/Singapore` - left alone. The bug only existed in the 4 endpoints (plus the Reports invoice fetch) that took an already-stringified `YYYY-MM-DD` and re-parsed it assuming UTC.
- **Used a fixed `+08:00` offset, not a timezone library.** Singapore Standard Time has no DST and the business only operates there (per `CLAUDE.md`), so a hardcoded offset is correct and simpler than pulling in a timezone-conversion dependency for one fixed, permanent offset.
- **Left `vendorExpenses`/`fleetOverview` untouched deliberately**, rather than applying the same helper everywhere for consistency - their date columns (`invoice_date`, `scheduled_date`) are DATE-only in Postgres, so a bare `YYYY-MM-DD` comparison against them has no time-of-day component to get wrong in the first place. Changing code that isn't broken would just be noise.
- **Ran both full Jest suites before considering this done**, not just a targeted subset - a timezone-boundary change is exactly the kind of edit whose blast radius isn't obvious from the diff alone (any test seeded with a specific "recent" timestamp could have silently depended on the old, wrong boundary).

### Files Changed

- `backend/src/controllers/dashboardController.js` - added `SGT_OFFSET`/`sgDayStart()`/`sgDayEnd()` helpers; applied them in place of the old `new Date(from)` / `new Date(`${to}T23:59:59.999Z`)` pattern in `revenueLeakage`, `cycleTime`, `revenueByServiceType`, and `leakageHistory`.
- `frontend/src/pages/dashboard/ReportPage.jsx` - the invoices-fetch effect now sends `dateRange.startDate.toISOString()`/`dateRange.endDate.toISOString()` instead of reconstructing an ISO string from `toYMD()` + a hardcoded `Z` suffix.

**Verification:** live Playwright reproduction (login as MD, compare raw invoice `created_at` values against the "Today" query window) before AND after the fix, confirming the exact before/after numbers ($0.00/0 invoices -> $65.40/3 invoices) on the Reports page; full backend Jest suite (43/43 suites, 468 tests) and full frontend Jest suite (32/32 suites, 223 tests), both green after the fix.

### What Still Needs to Be Done

- [ ] **Not committed/merged as of this handoff being written** - check the git log immediately after this entry for whether the commit-then-merge-to-main this session ended with actually landed cleanly.
- [ ] **No automated regression test added for this specific bug** (an invoice/milestone timestamped just after local midnight but before 8am should count under "Today"). Everything here was verified live and via the existing suites (which happened to still pass, but weren't testing this exact boundary before or after) - worth adding a dedicated test asserting the Singapore-local day boundary, so this can't silently regress if someone touches these date helpers later.
- [ ] **This fix assumes the deployment's OS/DB timezone situation stays like this dev machine's** (`Asia/Singapore`, confirmed via `Intl.DateTimeFormat().resolvedOptions().timeZone`). If EFAR is ever deployed to a server running a different system timezone (common for cloud VMs, which often default to UTC), `resolvePeriodRange`'s Fleet Overview path (untouched this session, since it works correctly here) would need the same `+08:00`-anchoring treatment this session applied elsewhere - it currently only works "by accident" because this machine's own clock is already Singapore time.
- [ ] **Items still open from Sessions 1-3** (no test coverage for the transfer-type pre-fill or map legend from Session 1, no live browser verification of those two, no functional forgot-password path from Sessions 2-3) remain open.

### Context for Next Session

- **`Asia/Singapore` (UTC+8, no DST) is the one true timezone this whole app should reason in** - both for display (already handled fine, since the browser's local clock is Singapore time on every real user's machine) and for any backend date-boundary math involving a TIMESTAMP column. `sgDayStart`/`sgDayEnd` in `dashboardController.js` are now the reusable pattern for this - reach for them (or lift them to a shared util if a 5th call site needs the same fix) rather than reintroducing a bare `new Date(dateOnlyString)` against a TIMESTAMP column.
- **DATE-only columns (`invoice_date`, `scheduled_date`) are immune to this whole class of bug** - they store a calendar date with no time-of-day, so comparing them against a `YYYY-MM-DD` string is always correct regardless of timezone. Only TIMESTAMP columns (`created_at`, `recorded_at`, `approved_at`, etc.) need the SGT-anchoring treatment.
- **Both dev servers had already stopped between the previous session and this one** (neither was killed intentionally - they'd just been closed or crashed) - this session started fresh copies for testing and stopped them again at the end. Nothing was left running.

---

## Session 5 - First Production Deploy to Vercel/Render: Pre-Deploy Audit, Then Three Live Bugs Fixed in Sequence

### What We Worked On

The app's first deployment to Vercel (frontend) + Render (backend) + Supabase (shared DB, same instance as local dev), done live over several rounds as each fix surfaced the next problem.

1. **Pre-deployment audit** (before anything was live) - reviewed the codebase against `design/architecture.md` for hardcoded URLs/credentials, CORS config, Sequelize's `DATABASE_URL` usage, Cloudinary config, and route error-handling coverage. Found everything already correctly env-driven and try/catch-covered; flagged (but didn't yet fix) several deploy-config gaps: no `vercel.json` SPA rewrite, an unused `VITE_GEMINI_API_KEY`, and `deployment.md` being stale (local-dev-only, missing the Vercel/Render/Supabase production picture).
2. **Rewrote `deployment.md`** per the user's spec - a Cloud Services table with public-URL placeholders (Frontend/Backend/Database/Image Storage), then full env var tables grouped by service, descriptions only, no real values.
3. **Fixed the health-check startup log** (`backend/src/index.js`) - it always printed `http://localhost:${PORT}/health` even in production. Now uses Render's auto-populated `RENDER_EXTERNAL_URL` when present. Cosmetic (the `/health` route itself always worked), but confusing in Render's logs.
4. **Bug 1 live: refreshing any non-root route 404'd on Vercel.** Root cause: no `vercel.json` rewrite, so Vercel's static host looked for a literal file/path instead of falling through to `index.html` for React Router to handle. Added `frontend/vercel.json` with a catch-all rewrite to `/index.html`.
5. **Bug 2 live: login showed `"Route POST /auth/login not found"`.** Root cause: `VITE_API_BASE_URL` was set on Vercel to the bare Render URL without the `/api` suffix, but every frontend API call is a relative path (`/auth/login`) that assumes the base already ends in `/api` (`frontend/src/api/index.js`). Fixed by setting `VITE_API_BASE_URL=https://full-stack-application-development.onrender.com/api` on Vercel.
6. **Bug 3 live: login showed `"Invalid email or password."` with correct credentials.** Diagnosed by testing the backend directly with `curl` first (confirmed the DB, password hash, and JWT signing were all correct - the demo account logged in fine over HTTP) rather than assuming the DB was the problem. Then reproduced the actual browser request with `curl -H "Origin: <vercel-preview-url>"` and found the mismatch: the user was testing on a Vercel **preview** URL (`...-git-7f2c43-secbest-hq.vercel.app`), but `FRONTEND_URL` on Render was pinned to the single production alias (`...-pi.vercel.app`). `cors()` with a static string origin always echoes that one configured value regardless of the request's real origin, so the browser's CORS check failed, the request never resolved to a response, and `LoginPage.jsx`'s catch-all fallback (`err.response?.data?.message || 'Invalid email or password.'`) displayed a misleading credentials-error message for what was actually a CORS rejection. Fixed by changing `backend/src/index.js`'s CORS `origin` from a fixed string to a function that accepts both the exact `FRONTEND_URL` and any origin matching `/^https:\/\/full-stack-application-development[a-z0-9-]*\.vercel\.app$/`, so every Vercel preview deployment works without updating `FRONTEND_URL` per-branch.
7. **Also fixed a related code smell found while investigating Bug 3**: `authController.js`'s `login` catch block returned `"Invalid email or password."` (401) for *any* unexpected error, including genuine server-side failures (DB connection errors, etc.) - meaning a real 500 would have been indistinguishable from a bad password with no server-side log to tell them apart. Changed it to log the real error (`console.error('[auth] login failed unexpectedly:', err)`) and return a proper 500/`INTERNAL_ERROR` for anything that isn't a validation error, so this can't happen silently again.
8. **Restricted the Live Fleet Tracker map to Singapore** (`frontend/src/components/FleetMap.jsx`) - added a `restriction` bounding box (covering the main island plus outlying islands like Sentosa/Pulau Ubin) with `strictBounds: true`, plus a `minZoom: 11` floor, so the map can't be panned or zoomed out past Singapore. Verified with `npm run build` before pushing since this was requested "safely" during active deployment.
9. **Updated `CLAUDE.md`** with a new "Deployment (Vercel + Render + Supabase)" section documenting the live URLs, the required deploy-time config (the `/api` suffix, the CORS regex, `NODE_ENV=production`, `vercel.json`, `XERO_SIMULATION`), and the three gotchas above so they're recognized immediately if they resurface (e.g. after a Vercel project rename, or a teammate's fresh deploy).
10. Every fix in this session was committed and pushed to `main` individually as it was diagnosed and resolved, matching the user's request to "push this main" / "commit and push to main safely" - see Files Changed below for the four resulting commits.

### Key Decisions Made

- **Test the backend directly with `curl` before assuming the database or credentials are wrong.** When "Invalid email or password" appeared despite correct-looking credentials, curling the live Render URL directly (bypassing the browser entirely) proved in one request that the DB/password/JWT path was fine - which immediately ruled out an entire category of possible causes and pointed straight at the network/CORS layer instead.
- **Reproduce the exact failure condition (the real `Origin` header), not just "does the endpoint work."** The first curl test (no `Origin` header) succeeded and could have been mistaken for "everything's fine" - only adding the actual Vercel preview URL as the `Origin` header surfaced the CORS mismatch that the browser was actually hitting.
- **Fix CORS to accept the whole preview-URL family via regex, not just add one more exact string.** A single additional hardcoded preview URL would break again on the next branch/commit. Matching `full-stack-application-development[a-z0-9-]*.vercel.app` covers every past and future preview deployment for this specific Vercel project without needing `FRONTEND_URL` updated per-branch.
- **Don't let a generic frontend error fallback mask the real failure category.** `LoginPage.jsx`'s `|| 'Invalid email or password.'` fallback is reasonable UX for an end user, but it cost real debugging time by making a CORS failure look identical to a credentials failure. Documented this explicitly in `CLAUDE.md` so the next debugging session goes straight to the Network tab instead of re-suspecting the DB.
- **Stop masking genuine 500s as 401s in `authController.login`'s catch block.** A real bug independent of anything the user reported - found only because investigating Bug 3 required reading that function closely. Fixed opportunistically since it directly affects how debuggable *any* future login failure will be, not just this session's.
- **Keep the Singapore map restriction change minimal and build-verified before pushing**, given explicit "I'm in deployment mode" framing - no env vars, API keys, or backend/CORS config touched; ran `npm run build` locally first to confirm no build breakage before committing.

### Files Changed

- `deployment.md` - rewritten: Cloud Services table with public-URL placeholders, full env var tables grouped by service (Frontend/Backend/Database/Image Storage), placeholder descriptions only. Later amended to spell out the `/api` suffix requirement on `VITE_API_BASE_URL`.
- `backend/src/index.js` - health-check startup log now uses `RENDER_EXTERNAL_URL` when present; CORS `origin` changed from a fixed string to a function matching `FRONTEND_URL` exactly or any `full-stack-application-development*.vercel.app` origin via regex.
- `backend/src/controllers/authController.js` - `login`'s catch block now logs unexpected errors and returns 500/`INTERNAL_ERROR` instead of masking them as 401/`INVALID_CREDENTIALS`.
- `frontend/vercel.json` - new file: catch-all rewrite (`/(.*)` -> `/index.html`) so client-side routing survives a page refresh on Vercel.
- `frontend/src/components/FleetMap.jsx` - added `SINGAPORE_BOUNDS`/`SINGAPORE_MIN_ZOOM` and wired them into the map's `restriction`/`minZoom` options.
- `CLAUDE.md` - new "Deployment (Vercel + Render + Supabase)" section: live URLs, required deploy-time config, and the three gotchas from this session.

Commits (all pushed to `main`): `f17e69a` (vercel.json + health-check log + deployment.md), `9d4e60f` (CORS regex + auth error-masking fix), `a6b1730` (Singapore map restriction).

**Verification:** Backend login tested directly via `curl` against the live Render URL, both without and with the `Origin` header set to the failing Vercel preview URL, confirming the CORS diagnosis before writing the fix. Frontend `npm run build` run and confirmed green before pushing the Fleet Map change. No automated test suite was run this session (no test files were touched), and no live browser click-through of the fixed login flow was done after the CORS fix shipped - see below.

### What Still Needs to Be Done

- [ ] **The CORS/login fix has not been re-verified live in a browser as of this handoff.** The `curl`-based diagnosis is strong evidence, but nobody has actually reloaded the Vercel preview URL and logged in through the UI since `9d4e60f` was pushed and Render redeployed. Do this first in the next session if login issues are reported again.
- [ ] **`VITE_GEMINI_API_KEY` is still listed in `frontend/.env.example` and `deployment.md` but has zero usages in `frontend/src`** (flagged during the pre-deploy audit, not yet removed). Low priority, but it's a dead env var that would expose a Gemini key client-side if anyone ever wired it up - recommend removing it from both files rather than leaving it as a landmine.
- [ ] **The Singapore Fleet Map restriction has not been visually verified in a browser** - only reasoned about via the Google Maps `restriction`/`minZoom` API and confirmed via `npm run build`. Worth a quick look to confirm panning/zooming actually clamps as expected and the initial fit-to-crew-bounds behavior still looks right within the restricted viewport.
- [ ] **No automated regression test exists for the CORS origin-matching function or the `/api`-suffix requirement.** Both are exactly the kind of deploy-config logic that silently breaks again if touched carelessly (e.g. someone "simplifying" the CORS function back to a plain string) - worth a small backend test asserting the regex matches known preview-URL shapes and rejects arbitrary origins.
- [ ] **Xero is confirmed live (`XERO_SIMULATION=false` with real credentials) in the local `.env`** - per earlier project notes (see prior handoffs / memory), Xero was previously pointed at "Demo Company (Global)" with no SG GST rates. Worth confirming which Xero org the *production* Render deployment is actually connected to before relying on live AR sync in a demo - this wasn't checked this session.
- [ ] **`deployment.md`'s Cloud Services table still has placeholder text (`_fill in after deploying_`) for the Frontend/Backend public URLs** rather than the actual live URLs now known (`https://full-stack-application-development-pi.vercel.app`, `https://full-stack-application-development.onrender.com`) - worth filling in now that both are confirmed live, so the doc matches `CLAUDE.md`'s new Deployment section instead of lagging behind it.

### Context for Next Session

- **This project's Vercel project name is `full-stack-application-development`** (confirmed from both the production alias `...-pi.vercel.app` and the preview URL `...-git-7f2c43-secbest-hq.vercel.app` sharing that prefix) - the CORS regex in `backend/src/index.js` is keyed to this exact prefix. If the Vercel project is ever renamed or a second Vercel project/custom domain is added for this app, the regex (and/or `FRONTEND_URL`) needs updating or new logins will hit the same CORS wall Bug 3 did.
- **The production Supabase database is the same one used for local dev** (`arorhvtvepwimqwhrzps`) - there is no separate prod-only database to seed or migrate. Any `db:setup`/migration run locally already applies to what's live, and any manual data cleanup done locally also affects the live app - worth keeping in mind before running destructive scripts (`db:reset`, `db:purge-seed`) locally during active deployment.
- **`CLAUDE.md` now has a "Deployment" section as the first stop for any future deploy-related question or bug** - check it before re-diagnosing the same three gotchas (SPA rewrite, `/api` suffix, CORS/preview-URL mismatch) from scratch.

---

