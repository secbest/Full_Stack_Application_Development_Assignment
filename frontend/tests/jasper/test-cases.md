# Frontend Test Cases - Jasper

_List all test cases below before submission. Include what is being tested and the expected outcome._

| # | Test File | What is Tested | Expected Outcome |
|---|-----------|---------------|-----------------|
| 1 | ContractFormPage.test.jsx | Create/Edit Contract form (UC-01/UC-02): `POST`/`PATCH /api/contracts`, mocked via axios-mock-adapter | Create: 201 shows a success toast (incl. "no rates" warning variant) and navigates to the new contract; 500/400/409 show the backend or fallback error message as a toast; empty required fields are blocked client-side by Formik/Yup with inline `FieldError` text and never call the API. Edit: loads the existing contract with no client picker or rates/surcharges cards; successful `PATCH` navigates to the contract; a `HAS_MATCHED_INVOICES` 400 shows a `window.confirm` re-prompt, resubmitting with `acknowledge_matched_invoices: true` on accept or sending nothing further on decline |
| 2 | PricingContractPage.test.jsx | Pricing Contracts List (screen 11): `GET /api/contracts`, filter tabs, search, navigation | Active/upcoming contracts show by default with expired/deactivated hidden behind an "N hidden" count; the "Show inactive" checkbox reveals the Expired/Deactivated tabs and rows; filter tabs and the search box narrow the visible rows; "New Contract"/"View" navigate correctly; a failed load shows an error toast and the empty state |
| 3 | ContractDetailPage.test.jsx | Pricing Contract Detail (screen 12): rate add/edit/delete, surcharge batch edit, deactivate (UC-02) | Loads header/rates/surcharges and the matched-invoice warning banner; adding a rate is blocked client-side when incomplete, posts correctly on success, and surfaces a 409 `RATE_DUPLICATE` as a toast; editing/deleting a rate PUTs/DELETEs correctly, with the delete confirm dialog respected; deactivating asks for confirmation and, on a `HAS_MATCHED_INVOICES` 400, shows a second confirm and resubmits with the acknowledgment flag (or sends nothing further if declined); surcharge batch save reports per-row failures via `Promise.allSettled`; expired contracts hide all edit/deactivate/add-rate controls |
| 4 | MyJobsPage.test.jsx | The current-job hero card and live milestone stepper (client feedback #1 and #3) | An `in_progress` job renders as the Current Job hero with the stepper, where the next milestone is the only tappable button and later stages are visible but inert; a `confirmed` job scheduled today within the next hour becomes the hero offering "Activated"; with nothing eligible the page shows "No active job right now" and auto-expands the upcoming list; `completed`/`invoiced` jobs never become the hero and read "Memo Submitted" with no create-memo button; tapping a milestone POSTs `{milestone_type}` to `/bookings/:id/milestone` and re-renders the stepper from the response; a 409 shows the backend message and leaves the button tappable; once `job_completed` is recorded the hero's Create Memo navigates to the wizard |
| 5 | MemoWizardStandby.test.jsx | Wizard milestone pre-fill and manpower-only standby support (client feedback #1 and #4) | Step 1 pre-fills `job_start_time`/`job_end_time` from the recorded `activated`/`job_completed` milestones converted to local time, and leaves them empty when no milestones exist; an `event_standby` booking labels both patient fields "(optional)", shows the manpower-only hint, and advances to Step 2 with them blank; an `eas` booking still blocks Step 1 with "Patient name is required"; Step 2 defaults `service_type` to the booking's own type |
| 6 | serviceMemoValidation.test.js (buildStep1Schema) | The frontend mirror of the backend's conditional patient-field rule (client feedback #4) | `buildStep1Schema('event_standby')` and `('workplace_standby')` accept blank patient fields; `('eas')` and `('mts')` still require them with the documented messages; the default `step1Schema` export keeps the ambulance (required) behaviour |
